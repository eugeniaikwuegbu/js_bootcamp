let myDetails = {
    name : 'Eugenia',
    age : 24,
    location : 'Nigeria'
}

console.log(`${myDetails.name} is ${myDetails.age} and lives in ${myDetails.location}`)

myDetails.age = myDetails.age + 1;
console.log(`${myDetails.name} is ${myDetails.age} and lives in ${myDetails.location}`)
